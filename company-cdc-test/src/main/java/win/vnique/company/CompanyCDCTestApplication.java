package win.vnique.company;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * The spring boot application class, that starts the app.
 *
 * @author Sohan
 */
@SpringBootApplication
public class CompanyCDCTestApplication {

	/**
	 * Main method that starts the Spring Boot application.
	 *
	 * @param args Arguments passed to the app.
	 */
	public static void main(String[] args) {
		SpringApplication.run(CompanyCDCTestApplication.class, args);
	}
}
